# Database
DATABASE_URL="postgresql://deepdive:deepdive_dev@localhost:5432/deepdive"

# Session secret – generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET="replace-with-64-byte-hex-secret"

# SMTP (Mailpit for development)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_FROM="noreply@deepest-dive.local"

# Application base URL
APP_BASE_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"
